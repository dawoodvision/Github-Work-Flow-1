name: My First Workflow

on:
  push: 
    branches: [ main ]

jobs:
  run:
  runs-on: ubuntu-latest
  
  steps:
    -name: Get Code
      uses: actions/checkout@v3 

    -name: Say Hello  
      run: echo echo "Hello, workflow is working"